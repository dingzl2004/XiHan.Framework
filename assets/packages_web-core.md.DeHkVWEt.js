import{_ as i,o as s,c as a,a3 as t}from"./chunks/framework.B5WAEDB7.js";const k=JSON.parse('{"title":"XiHan.Framework.Web.Core","description":"","frontmatter":{},"headers":[],"relativePath":"packages/web-core.md","filePath":"packages/web-core.md","lastUpdated":1786268168000}'),n={name:"packages/web-core.md"};function o(d,e,r,l,c,p){return s(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="xihan-framework-web-core" tabindex="-1">XiHan.Framework.Web.Core <a class="header-anchor" href="#xihan-framework-web-core" aria-label="Permalink to &quot;XiHan.Framework.Web.Core&quot;">​</a></h1><blockquote><p>Web 基础设施底座：托管环境接入、HttpContext 支持、当前主体桥接（<code>ICurrentUser</code>）、以及客户端信息识别（真实 IP / 归属地 / 浏览器·系统·设备）。</p></blockquote><ul><li><strong>NuGet</strong>：<code>XiHan.Framework.Web.Core</code></li><li><strong>模块类</strong>：<code>XiHanWebCoreModule</code></li><li><strong>所在层</strong>：Web 层</li><li><strong>关键依赖</strong>：<code>IP2Region.Net</code>（离线 IP 归属地）、<code>UAParser</code>（User-Agent 解析）；SDK 为 <code>Microsoft.NET.Sdk.Web</code>，可直接使用 ASP.NET Core 类型。</li></ul><h2 id="概述" tabindex="-1">概述 <a class="header-anchor" href="#概述" aria-label="Permalink to &quot;概述&quot;">​</a></h2><p>XiHan.Framework.Web.Core 是所有 Web 相关包（Web.Api、Web.Docs、Web.Grpc、Web.RealTime、Web.Gateway）的公共底座。它把「当前 HTTP 请求」这件事在框架里落地为几件具体的基础设施：</p><ul><li>把 ASP.NET Core 的 <code>IWebHostEnvironment</code> 环境名同步进框架的 <code>IXiHanHostEnvironment</code>；</li><li>注册 <code>IHttpContextAccessor</code>，让服务层随处可拿到当前请求；</li><li>用 <code>HttpContext.User</code>（JWT 认证后的主体）作为框架 <code>ICurrentUser</code> / <code>ICurrentPrincipalAccessor</code> 的数据源；</li><li>提供一个「客户端信息识别」服务，从请求里解析出真实客户端 IP、地理归属地、浏览器 / 操作系统 / 设备名。</li></ul><p>你一般不直接引用本包，而是通过上层的 Web.Api 等包间接获得它。</p><h2 id="何时使用" tabindex="-1">何时使用 <a class="header-anchor" href="#何时使用" aria-label="Permalink to &quot;何时使用&quot;">​</a></h2><ul><li>需要在服务里拿到「当前登录用户」——本包让框架 <code>ICurrentUser</code> 在 Web 请求中可用（读取 <code>HttpContext.User</code>）。</li><li>需要识别请求来源：穿透反向代理还原真实客户端 IP、解析归属地、识别浏览器与设备（登录日志、审计、风控常用）。</li><li>需要一个统一的 <code>IApplicationBuilder</code> / 托管环境访问入口，供模块化生命周期（<code>OnApplicationInitialization</code>）使用。</li></ul><h2 id="安装与启用" tabindex="-1">安装与启用 <a class="header-anchor" href="#安装与启用" aria-label="Permalink to &quot;安装与启用&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">dotnet</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> package</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> XiHan.Framework.Web.Core</span></span></code></pre></div><div class="language-csharp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">csharp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">DependsOn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">typeof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">XiHanWebCoreModule</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))]</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> MyModule</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> : </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">XiHanModule</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { }</span></span></code></pre></div><p>模块 <code>PreConfigureServices</code> 会把 <code>IWebHostEnvironment.EnvironmentName</code> 同步进 <code>IXiHanHostEnvironment</code>（当后者为空时）；<code>ConfigureServices</code> 调用 <code>AddXiHanWebCore(configuration)</code>，注册：<code>IApplicationBuilder</code> 的 <code>ObjectAccessor</code>、<code>IHttpContextAccessor</code>、<code>XiHanClientInfoOptions</code>（绑定配置节）、<code>IClientInfoProvider</code>（单例）、以及 <code>ICurrentPrincipalAccessor</code>（Scoped，实现为 <code>HttpContextCurrentPrincipalAccessor</code>）。</p><h2 id="核心能力" tabindex="-1">核心能力 <a class="header-anchor" href="#核心能力" aria-label="Permalink to &quot;核心能力&quot;">​</a></h2><ul><li><strong>托管环境接入</strong>：模块前置阶段把 ASP.NET Core 环境名写入 <code>IXiHanHostEnvironment</code>。若 DI 中缺 <code>IWebHostEnvironment</code>，<code>GetHostingEnvironment()</code> 回退到 <code>EmptyHostingEnvironment</code>（默认 <code>Development</code>）。</li><li><strong>HttpContext 与当前主体</strong>：注册 <code>IHttpContextAccessor</code>；<code>HttpContextCurrentPrincipalAccessor</code> 以 <code>HttpContext.User</code> 为当前主体（无请求时回退空 <code>ClaimsPrincipal</code>），从而让框架 <code>ICurrentUser</code> 在 Web 请求中生效。</li><li><strong>客户端信息识别</strong>：<code>IClientInfoProvider.GetCurrent()</code> 从当前请求解析出 <code>ClientInfo</code>（IP / 归属地 / 原始 UA / 浏览器 / 系统 / 设备）。</li><li><strong>真实 IP 解析</strong>：优先取 <code>X-Forwarded-For</code> 第一跳，其次 <code>X-Real-IP</code>，回退 <code>Connection.RemoteIpAddress</code>；对 IPv4-mapped-IPv6 做规范化（<code>MapToIPv4</code>）。</li><li><strong>IP 归属地定位</strong>：回环 IP 记为「本机」、内网 IP 记为「局域网」；仅公网 IP 才触发 <code>ip2region</code>（<code>IP2Region.Net</code>）离线查询。数据库文件按候选路径自动探测，缺库时静默跳过、不报错。</li><li><strong>UA 解析</strong>：基于 <code>UAParser</code> 解析 User-Agent 得到浏览器 / 系统 / 设备名；桌面浏览器 <code>Device.Family</code> 常为 <code>Other</code>，会归一化为 <code>PC</code>。</li><li><strong>Claims 转换（可选）</strong>：<code>XiHanClaimsTransformation</code> 支持把标准 OIDC 声明（<code>sub</code>/<code>role</code>/<code>email</code>/<code>name</code> 等）映射为框架 <code>XiHanClaimTypes</code>；通过 <code>services.TransformXiHanClaims()</code> 显式注册后才生效。</li></ul><h2 id="主要-api-类型" tabindex="-1">主要 API / 类型 <a class="header-anchor" href="#主要-api-类型" aria-label="Permalink to &quot;主要 API / 类型&quot;">​</a></h2><table tabindex="0"><thead><tr><th>类型</th><th>说明</th></tr></thead><tbody><tr><td><code>IClientInfoProvider</code></td><td>客户端信息提供器契约。唯一方法 <code>ClientInfo GetCurrent()</code>。</td></tr><tr><td><code>ClientInfo</code></td><td>结果模型：<code>IpAddress</code> / <code>Location</code> / <code>UserAgent</code> / <code>Browser</code> / <code>OperatingSystem</code> / <code>DeviceName</code>（均可空）。</td></tr><tr><td><code>HttpContextClientInfoProvider</code></td><td>默认实现（单例，<code>IDisposable</code>）：真实 IP 解析 + ip2region 归属地 + UAParser 解析。IP 查询器懒加载并加锁复用。</td></tr><tr><td><code>XiHanClientInfoOptions</code></td><td>客户端信息配置（配置节 <code>XiHan:Web:Core:ClientInfo</code>）。</td></tr><tr><td><code>HttpContextCurrentPrincipalAccessor</code></td><td>继承 <code>CurrentPrincipalAccessorBase</code>，以 <code>HttpContext.User</code> 作为当前主体，桥接框架 <code>ICurrentUser</code>。</td></tr><tr><td><code>XiHanClaimsTransformation</code></td><td><code>IClaimsTransformation</code> 实现，按映射表补加声明标识。</td></tr><tr><td><code>XiHanClaimsMapOptions</code></td><td>声明映射表 <code>Dictionary&lt;string, Func&lt;string&gt;&gt;</code>，默认映射 <code>sub/role/email/name/family_name/given_name</code>。</td></tr><tr><td><code>EmptyHostingEnvironment</code></td><td><code>IWebHostEnvironment</code> 空实现，DI 缺失环境时的回退。</td></tr></tbody></table><p>扩展方法（<code>XiHanWebCoreServiceCollectionExtensions</code>）：</p><table tabindex="0"><thead><tr><th>方法</th><th>说明</th></tr></thead><tbody><tr><td><code>services.AddXiHanWebCore(configuration)</code></td><td>注册本包全部核心服务（模块内部调用）。</td></tr><tr><td><code>services.GetHostingEnvironment()</code></td><td>取 <code>IWebHostEnvironment</code>，缺失时回退 <code>EmptyHostingEnvironment</code>。</td></tr><tr><td><code>services.TransformXiHanClaims()</code></td><td>注册 <code>IClaimsTransformation</code> = <code>XiHanClaimsTransformation</code>（声明映射，需显式调用）。</td></tr></tbody></table><p><code>ApplicationInitializationContext</code> 扩展（<code>ApplicationInitializationContextExtensions</code>）：<code>GetApplicationBuilder()</code> / <code>GetApplicationBuilderOrNull()</code> / <code>GetEnvironment()</code> / <code>GetEnvironmentOrNull()</code> / <code>GetConfiguration()</code> / <code>GetLoggerFactory()</code>——供模块在 <code>OnApplicationInitialization</code> 里取到 <code>IApplicationBuilder</code> 与环境。</p><p><code>WebApplicationBuilder</code> 扩展（<code>WebApplicationBuilderExtensions</code>）：<code>AddApplicationAsync&lt;TStartupModule&gt;(...)</code> / <code>AddApplicationAsync(startupModuleType, ...)</code>——在 <code>WebApplicationBuilder</code> 上装配模块化应用，自动接管 <code>Configuration</code> 与 <code>EnvironmentName</code>。</p><p><code>IApplicationBuilder</code> 扩展（<code>XiHanApplicationBuilderExtensions</code>）：<code>InitializeApplicationAsync()</code> / <code>InitializeApplication()</code>——驱动模块 <code>OnApplicationInitialization</code>，并挂接应用生命周期的关闭与释放。</p><h2 id="配置" tabindex="-1">配置 <a class="header-anchor" href="#配置" aria-label="Permalink to &quot;配置&quot;">​</a></h2><p>配置节：<code>XiHan:Web:Core:ClientInfo</code>（<code>XiHanClientInfoOptions</code>）。</p><table tabindex="0"><thead><tr><th>字段</th><th>类型</th><th>默认值</th><th>含义</th></tr></thead><tbody><tr><td><code>EnableIpRegion</code></td><td><code>bool</code></td><td><code>true</code></td><td>是否对公网 IP 启用 ip2region 归属地解析。</td></tr><tr><td><code>Ip2RegionDbPath</code></td><td><code>string?</code></td><td><code>IpDatabases/ip2region.xdb</code></td><td>ip2region xdb 文件路径，支持相对路径（相对 <code>ContentRootPath</code>）。</td></tr></tbody></table><p>数据库候选路径顺序：配置的 <code>Ip2RegionDbPath</code> → <code>IpDatabases/ip2region.xdb</code> → <code>ip2region.xdb</code>；逐个探测存在的文件，都不存在时静默跳过归属地解析。</p><p>示例 <code>appsettings.json</code>：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;XiHan&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;Web&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;Core&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;ClientInfo&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">          &quot;EnableIpRegion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">          &quot;Ip2RegionDbPath&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;IpDatabases/ip2region.xdb&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h2 id="使用示例" tabindex="-1">使用示例 <a class="header-anchor" href="#使用示例" aria-label="Permalink to &quot;使用示例&quot;">​</a></h2><p>在任意服务里注入 <code>IClientInfoProvider</code> 拿到当前请求的客户端信息：</p><div class="language-csharp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">csharp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LoginLogWriter</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    private</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> readonly</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> IClientInfoProvider</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> _clientInfoProvider</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LoginLogWriter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">IClientInfoProvider</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> clientInfoProvider</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        =&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> _clientInfoProvider </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> clientInfoProvider;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Record</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">string</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> userName</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        var</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> info</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> _clientInfoProvider.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">GetCurrent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // info.IpAddress / info.Location / info.Browser / info.OperatingSystem / info.DeviceName</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>启用 OIDC 声明映射（把 <code>sub</code>/<code>role</code>/<code>email</code> 等映射为框架声明类型）：</p><div class="language-csharp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">csharp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> override</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ConfigureServices</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ServiceConfigurationContext</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> context</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    context.Services.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TransformXiHanClaims</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h2 id="扩展点-自定义" tabindex="-1">扩展点 / 自定义 <a class="header-anchor" href="#扩展点-自定义" aria-label="Permalink to &quot;扩展点 / 自定义&quot;">​</a></h2><ul><li><strong>替换客户端信息提供器</strong>：<code>IClientInfoProvider</code> 以 <code>AddSingleton</code>（非 <code>TryAdd</code>）注册默认实现；如需自定义解析逻辑，可在模块配置阶段用自己的实现覆盖注册（后注册者胜出），或在此基础上包装。</li><li><strong>替换当前主体来源</strong>：<code>ICurrentPrincipalAccessor</code> 以 Scoped 注册 <code>HttpContextCurrentPrincipalAccessor</code>；非标准场景可替换实现改变「当前主体」的取值来源。</li><li><strong>自定义声明映射</strong>：<code>Configure&lt;XiHanClaimsMapOptions&gt;</code> 修改 <code>Maps</code> 字典（键=源声明类型，值=返回目标 <code>XiHanClaimTypes</code> 的委托），再 <code>TransformXiHanClaims()</code> 注册。</li></ul><h2 id="注意事项与最佳实践" tabindex="-1">注意事项与最佳实践 <a class="header-anchor" href="#注意事项与最佳实践" aria-label="Permalink to &quot;注意事项与最佳实践&quot;">​</a></h2><ul><li><strong>归属地依赖离线库</strong>：<code>ip2region.xdb</code> 需随应用部署（默认放 <code>IpDatabases/</code>）。缺库不会报错，但 <code>ClientInfo.Location</code> 对公网 IP 为 <code>null</code>。</li><li><strong>真实 IP 依赖转发头还原</strong>：X-Forwarded-For 由上层 Web.Api 的 <code>UseForwardedHeaders</code> 在管线最前还原；反向代理需正确透传该头，否则解析到的是反代地址。</li><li><strong><code>ICurrentUser</code> 生效前提</strong>：本包只桥接 <code>HttpContext.User</code>；用户主体的真实内容由认证中间件（JWT）填充，须在 Web.Api 中启用认证。</li><li><strong>声明转换不默认启用</strong>：<code>XiHanClaimsTransformation</code> 只有显式调用 <code>TransformXiHanClaims()</code> 才注册；未调用时不做任何声明映射。</li></ul><h2 id="依赖模块" tabindex="-1">依赖模块 <a class="header-anchor" href="#依赖模块" aria-label="Permalink to &quot;依赖模块&quot;">​</a></h2><ul><li>内部依赖：<a href="./core">XiHan.Framework.Core</a>、<a href="./authentication">XiHan.Framework.Authentication</a>、<a href="./security">XiHan.Framework.Security</a>。</li><li>第三方核心：<code>IP2Region.Net</code>（离线 IP 归属地）、<code>UAParser</code>（User-Agent 解析）。</li></ul><h2 id="相关模块" tabindex="-1">相关模块 <a class="header-anchor" href="#相关模块" aria-label="Permalink to &quot;相关模块&quot;">​</a></h2><ul><li><a href="./web-api">XiHan.Framework.Web.Api</a> — 在本包之上装配完整的 REST API 中间件管道。</li><li><a href="./web-docs">XiHan.Framework.Web.Docs</a>、<a href="./web-realtime">XiHan.Framework.Web.RealTime</a>、<a href="./web-grpc">XiHan.Framework.Web.Grpc</a>、<a href="./web-gateway">XiHan.Framework.Web.Gateway</a> — 其它 Web 层包，均以本包为底座。</li><li><a href="./../guide/lifecycle">模块生命周期</a> — <code>PreConfigureServices</code> / <code>ConfigureServices</code> / <code>OnApplicationInitialization</code> 钩子。</li></ul>`,41)])])}const g=i(n,[["render",o]]);export{k as __pageData,g as default};
