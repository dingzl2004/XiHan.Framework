import{_ as i,o as s,c as t,a3 as o}from"./chunks/framework.BLaSdaBb.js";const k=JSON.parse('{"title":"XiHan.Framework.Authorization","description":"","frontmatter":{},"headers":[],"relativePath":"packages/authorization.md","filePath":"packages/authorization.md","lastUpdated":1786268168000}'),a={name:"packages/authorization.md"};function d(c,e,n,r,l,h){return s(),t("div",null,[...e[0]||(e[0]=[o(`<h1 id="xihan-framework-authorization" tabindex="-1">XiHan.Framework.Authorization <a class="header-anchor" href="#xihan-framework-authorization" aria-label="Permalink to &quot;XiHan.Framework.Authorization&quot;">​</a></h1><blockquote><p>授权：RBAC 角色权限、Policy 策略、ABAC 属性访问控制，并接入 ASP.NET Core 授权管道（实时校验）</p></blockquote><ul><li><strong>NuGet</strong>：<code>XiHan.Framework.Authorization</code></li><li><strong>模块类</strong>：<code>XiHanAuthorizationModule</code>（<code>DependsOn</code> <code>XiHanAuthenticationModule</code>）</li><li><strong>所在层</strong>：基础设施层</li><li><strong>关键依赖</strong>：<code>FrameworkReference</code> <strong>Microsoft.AspNetCore.App</strong>（ASP.NET Core 授权管道）+ 框架内部依赖 Core / Authentication</li></ul><h2 id="概述" tabindex="-1">概述 <a class="header-anchor" href="#概述" aria-label="Permalink to &quot;概述&quot;">​</a></h2><p>这个包负责<strong>授权</strong>——判定“你能做什么”。它把三种主流授权模型整合到一起：<strong>RBAC</strong>（基于角色的权限）、<strong>Policy</strong>（策略）与 <strong>ABAC</strong>（基于属性的访问控制），并接入 ASP.NET Core 的授权管道，让你用一个 <code>[PermissionAuthorize(...)]</code> 特性就能触发“权限码 + 属性约束”的混合判定。</p><blockquote><p>认证（Authentication）= 你是谁；授权（Authorization）= 你能做什么。本包处理后者，建立在 <a href="./authentication">Authentication</a> 颁发的身份声明之上。</p></blockquote><p>关键设计取向是<strong>实时校验</strong>：授权处理器不信任 JWT 里冻结的权限声明，而是每次都走 <code>IPermissionChecker</code> 查当前授权快照，确保授权/撤销、用户禁用、会话注销即时生效。</p><h2 id="何时使用" tabindex="-1">何时使用 <a class="header-anchor" href="#何时使用" aria-label="Permalink to &quot;何时使用&quot;">​</a></h2><ul><li>需要基于角色 / 权限码的访问控制（RBAC）</li><li>需要在权限之上叠加属性约束（同租户、仅本人、时间、IP、资源属性等 ABAC）</li><li>需要把权限判定接进 ASP.NET Core，用特性声明式地保护 Controller/Action</li><li>需要“实时”授权：权限授予/回收、用户禁用即时生效，而非等令牌过期</li></ul><h2 id="安装与启用" tabindex="-1">安装与启用 <a class="header-anchor" href="#安装与启用" aria-label="Permalink to &quot;安装与启用&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">dotnet</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> package</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> XiHan.Framework.Authorization</span></span></code></pre></div><div class="language-csharp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">csharp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">DependsOn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">typeof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">XiHanAuthorizationModule</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))]</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> MyModule</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> : </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">XiHanModule</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { }</span></span></code></pre></div><p>模块 <code>ConfigureServices</code> 调用 <code>AddXiHanAuthorization(config)</code>，用 <code>TryAdd</code> 注册整套存储 / 评估器 / 服务，以及 ASP.NET Core 集成件：</p><ul><li>存储：<code>IRoleStore</code> → <code>DefaultRoleStore</code>、<code>IPermissionStore</code> → <code>DefaultPermissionStore</code>、<code>IPolicyStore</code> → <code>DefaultPolicyStore</code>（均 Scoped）</li><li>评估：<code>IPermissionChecker</code> → <code>DefaultPermissionChecker</code>、<code>IPolicyEvaluator</code> → <code>DefaultPolicyEvaluator</code>、<code>IAbacAttributeCollector</code> → <code>DefaultAbacAttributeCollector</code>、<code>IAbacEvaluator</code> → <code>DefaultAbacEvaluator</code>（均 Scoped）</li><li>门面：<code>IAuthorizationService</code> → <code>DefaultAuthorizationService</code>（Scoped）</li><li>ASP.NET Core：<code>IAuthorizationPolicyProvider</code> → <code>HybridPermissionPolicyProvider</code>（Singleton）、<code>IAuthorizationHandler</code> += <code>HybridPermissionAuthorizationHandler</code>（Scoped，<code>TryAddEnumerable</code>）</li></ul><h2 id="工作原理" tabindex="-1">工作原理 <a class="header-anchor" href="#工作原理" aria-label="Permalink to &quot;工作原理&quot;">​</a></h2><p><strong>混合策略名（约定核心）</strong>：特性把“权限码 + ABAC 策略码”编码进一个 ASP.NET Core 策略名，格式：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>xihan.hybrid:p={URL编码的权限码};a={URL编码的ABAC策略码}</span></span></code></pre></div><p><code>HybridPermissionPolicyProvider</code> 拦截以 <code>xihan.hybrid:</code> 开头的策略名，解析出两个编码后动态构建一条含 <code>HybridPermissionRequirement</code> 的策略（其余策略名回落到 <code>DefaultAuthorizationPolicyProvider</code>）。因此<strong>无需预注册命名策略</strong>，任意权限码/ABAC 组合即取即用。</p><p><strong>判定顺序（<code>HybridPermissionAuthorizationHandler</code>）</strong>：</p><ol><li>从主体声明解析 userId（<code>NameIdentifier</code> / <code>sub</code> / <code>userid</code> / <code>user_id</code>）；解析不到直接不通过。</li><li>若有权限码 → <code>IPermissionChecker.IsGrantedAsync</code> 实时校验（超管的通配 <code>*</code> 也由检查器的快照负责）；不通过则终止。</li><li>若无 ABAC 策略码 → 直接 <code>Succeed</code>。</li><li>若有 ABAC 策略码 → <code>IAbacAttributeCollector</code> 收集主体/资源/环境属性 → <code>IAbacEvaluator.EvaluateAsync</code> 评估 → 允许才 <code>Succeed</code>。</li></ol><p><strong>RBAC 权限来源（<code>DefaultPermissionChecker</code>）</strong>：<code>IsGrantedAsync</code> 先查用户直接权限，再遍历用户的启用角色查角色权限，任一命中（且 <code>IsEnabled</code>）即通过。<code>GetGrantedPermissionsAsync</code> 合并去重用户+角色权限。真实权限数据由 <code>IPermissionStore</code> / <code>IRoleStore</code> 提供（默认实现为线程安全的纯内存实现，进程重启即丢失且不预置任何数据，生产需业务层替换为读库实现）。</p><h2 id="核心能力" tabindex="-1">核心能力 <a class="header-anchor" href="#核心能力" aria-label="Permalink to &quot;核心能力&quot;">​</a></h2><ul><li><strong>RBAC</strong> <code>IRoleStore</code> + <code>RoleDefinition</code>：角色定义与用户-角色关系；<code>IAuthorizationService</code> 提供加入/移除角色、查询用户角色</li><li><strong>权限检查</strong> <code>IPermissionChecker</code>：<code>IsGrantedAsync</code>（单个）、<code>IsAnyGrantedAsync</code>（任一）、<code>IsAllGrantedAsync</code>（全部）、<code>GetGrantedPermissionsAsync</code>（列全部）、<code>PermissionExistsAsync</code></li><li><strong>Policy 策略</strong> <code>IPolicyEvaluator</code> + <code>PolicyDefinition</code>：可组合所需角色（任一）、权限（全部）、声明键值、自定义 <code>IAuthorizationRequirement</code>；支持 <code>EvaluateAll</code> / <code>EvaluateAny</code></li><li><strong>ABAC 属性控制</strong> <code>IAbacEvaluator</code> + <code>IAbacAttributeCollector</code>：评估上下文含主体 / 资源 / 环境三类属性（<code>AbacAttributeSet</code>）；默认评估器内置策略词汇（见下）</li><li><strong>ASP.NET Core 集成</strong>：<code>PermissionAuthorizeAttribute</code> / <code>AbacAuthorizeAttribute</code> + <code>HybridPermissionPolicyProvider</code> + <code>HybridPermissionAuthorizationHandler</code>，用标准 <code>[Authorize]</code> 管道跑混合判定</li><li><strong>实时授权</strong>：默认走 <code>IPermissionChecker</code> 实时校验，不信任 token 里冻结的权限声明；超管通配由检查器快照承载</li></ul><h2 id="主要-api-类型" tabindex="-1">主要 API / 类型 <a class="header-anchor" href="#主要-api-类型" aria-label="Permalink to &quot;主要 API / 类型&quot;">​</a></h2><h3 id="门面与结果" tabindex="-1">门面与结果 <a class="header-anchor" href="#门面与结果" aria-label="Permalink to &quot;门面与结果&quot;">​</a></h3><table tabindex="0"><thead><tr><th>类型</th><th>说明</th></tr></thead><tbody><tr><td><code>IAuthorizationService</code> / <code>DefaultAuthorizationService</code></td><td>授权总入口：<code>AuthorizeAsync</code>（权限）、<code>AuthorizePolicyAsync</code>（策略+资源）、<code>AuthorizeRoleAsync</code>、<code>AuthorizeAnyAsync</code> / <code>AuthorizeAllAsync</code>、<code>GetUserPermissionsAsync</code> / <code>GetUserRolesAsync</code>、<code>GrantPermissionAsync</code> / <code>RevokePermissionAsync</code>、<code>AddUserToRoleAsync</code> / <code>RemoveUserFromRoleAsync</code>；均返回 <code>AuthorizationResult</code></td></tr><tr><td><code>AuthorizationResult</code></td><td><code>Succeeded</code>、<code>FailureReason</code>、<code>FailedRequirements</code>、<code>AdditionalData</code>；工厂 <code>Success()</code> / <code>Failure(...)</code> / <code>PermissionDenied(name)</code> / <code>RoleDenied(name)</code></td></tr></tbody></table><h3 id="rbac" tabindex="-1">RBAC <a class="header-anchor" href="#rbac" aria-label="Permalink to &quot;RBAC&quot;">​</a></h3><table tabindex="0"><thead><tr><th>类型</th><th>关键方法 / 字段</th></tr></thead><tbody><tr><td><code>IPermissionChecker</code></td><td><code>Task&lt;bool&gt; IsGrantedAsync(userId, permissionName, ct)</code>、<code>IsAnyGrantedAsync(userId, List&lt;string&gt;, ct)</code>、<code>IsAllGrantedAsync(...)</code>、<code>Task&lt;List&lt;string&gt;&gt; GetGrantedPermissionsAsync(userId, ct)</code>、<code>Task&lt;bool&gt; PermissionExistsAsync(name, ct)</code></td></tr><tr><td><code>IPermissionStore</code></td><td>用户/角色权限读取、授予/撤销、<code>GetAllPermissionsAsync</code>、<code>GetPermissionByNameAsync</code></td></tr><tr><td><code>PermissionDefinition</code></td><td><code>Name</code>（唯一）、<code>DisplayName</code>、<code>Description</code>、<code>ParentName</code>、<code>Tag</code>、<code>IsEnabled</code>、<code>Order</code>、<code>Properties</code></td></tr><tr><td><code>IRoleStore</code></td><td><code>GetUserRolesAsync</code>、<code>IsInRoleAsync</code>、<code>AddUserToRoleAsync</code> / <code>RemoveUserFromRoleAsync</code>、<code>GetAllRolesAsync</code>、<code>GetRoleByName/IdAsync</code>、<code>Create/Update/DeleteRoleAsync</code>、<code>GetUsersInRoleAsync</code></td></tr><tr><td><code>RoleDefinition</code></td><td><code>Id</code>、<code>Name</code>（唯一）、<code>DisplayName</code>、<code>Description</code>、<code>IsEnabled</code>、<code>IsDefault</code>、<code>IsStatic</code>、<code>Order</code>、<code>CreatedTime</code> / <code>LastModifiedTime</code>、<code>Properties</code></td></tr></tbody></table><h3 id="policy" tabindex="-1">Policy <a class="header-anchor" href="#policy" aria-label="Permalink to &quot;Policy&quot;">​</a></h3><table tabindex="0"><thead><tr><th>类型</th><th>关键方法 / 字段</th></tr></thead><tbody><tr><td><code>IPolicyEvaluator</code></td><td><code>Task&lt;PolicyEvaluationResult&gt; EvaluateAsync(userId, policyName, resource?, ct)</code>、<code>EvaluateAllAsync(...)</code>、<code>EvaluateAnyAsync(...)</code></td></tr><tr><td><code>PolicyDefinition</code></td><td><code>Name</code>、<code>RequiredRoles</code>（任一）、<code>RequiredPermissions</code>（全部）、<code>RequiredClaims</code>（键值）、<code>CustomRequirements</code>（<code>List&lt;IAuthorizationRequirement&gt;</code>）、<code>IsEnabled</code></td></tr><tr><td><code>IAuthorizationRequirement</code></td><td>自定义要求：<code>string Name { get; }</code>、<code>Task&lt;bool&gt; EvaluateAsync(AuthorizationContext)</code></td></tr><tr><td><code>AuthorizationContext</code></td><td><code>UserId</code>、<code>UserRoles</code>、<code>UserPermissions</code>、<code>UserClaims</code>、<code>Resource</code>、<code>PolicyName</code>、<code>AdditionalData</code></td></tr></tbody></table><blockquote><p>注意：本包自有的 <code>IAuthorizationRequirement</code>（<code>XiHan.Framework.Authorization.Policies</code>）是 Policy 子系统的自定义要求接口，<strong>不同于</strong> ASP.NET Core 的同名接口 <code>Microsoft.AspNetCore.Authorization.IAuthorizationRequirement</code>（后者用于 <code>HybridPermissionRequirement</code>）。</p></blockquote><h3 id="abac" tabindex="-1">ABAC <a class="header-anchor" href="#abac" aria-label="Permalink to &quot;ABAC&quot;">​</a></h3><table tabindex="0"><thead><tr><th>类型</th><th>关键方法 / 字段</th></tr></thead><tbody><tr><td><code>IAbacEvaluator</code> / <code>DefaultAbacEvaluator</code></td><td><code>Task&lt;AbacEvaluationResult&gt; EvaluateAsync(AbacEvaluationContext, ct)</code></td></tr><tr><td><code>IAbacAttributeCollector</code> / <code>DefaultAbacAttributeCollector</code></td><td><code>Task&lt;AbacAttributeSet&gt; CollectAsync(ClaimsPrincipal, resource, permissionCode, policyCode, ct)</code></td></tr><tr><td><code>AbacAttributeSet</code></td><td><code>SubjectAttributes</code> / <code>ResourceAttributes</code> / <code>EnvironmentAttributes</code>（三个 <code>Dictionary&lt;string, object?&gt;</code>，键不区分大小写）</td></tr><tr><td><code>AbacEvaluationContext</code></td><td><code>UserId</code>、<code>PermissionCode</code>、<code>PolicyCode</code>、<code>Resource</code>、三类只读属性字典、<code>EvaluationTime</code></td></tr><tr><td><code>AbacEvaluationResult</code></td><td><code>IsAllowed</code>、<code>Reason</code>；工厂 <code>Allow(reason?)</code> / <code>Deny(reason?)</code></td></tr></tbody></table><h3 id="asp-net-core-集成" tabindex="-1">ASP.NET Core 集成 <a class="header-anchor" href="#asp-net-core-集成" aria-label="Permalink to &quot;ASP.NET Core 集成&quot;">​</a></h3><table tabindex="0"><thead><tr><th>类型</th><th>说明</th></tr></thead><tbody><tr><td><code>PermissionAuthorizeAttribute</code></td><td><code>[PermissionAuthorize(&quot;权限码&quot;)]</code> 或 <code>[PermissionAuthorize(&quot;权限码&quot;, &quot;abac策略码&quot;)]</code>；构造时即算出 <code>Policy = xihan.hybrid:p=...;a=...</code></td></tr><tr><td><code>AbacAuthorizeAttribute</code></td><td><code>[AbacAuthorize(&quot;abac策略码&quot;)]</code>——仅 ABAC，不校验 RBAC 权限码</td></tr><tr><td><code>HybridPermissionRequirement</code></td><td><code>PermissionCode</code>、<code>AbacPolicyCode</code>（实现 ASP.NET Core <code>IAuthorizationRequirement</code>）</td></tr><tr><td><code>HybridPermissionPolicyProvider</code></td><td>拦截 <code>xihan.hybrid:</code> 前缀策略名，动态构建策略；其余回落默认提供器</td></tr><tr><td><code>HybridPermissionAuthorizationHandler</code></td><td>混合判定处理器（先权限码实时校验，再 ABAC 评估）</td></tr></tbody></table><h2 id="默认-abac-策略词汇" tabindex="-1">默认 ABAC 策略词汇 <a class="header-anchor" href="#默认-abac-策略词汇" aria-label="Permalink to &quot;默认 ABAC 策略词汇&quot;">​</a></h2><p><code>DefaultAbacEvaluator</code> 内置一套开箱即用的策略码解析（<code>policyCode</code> 大小写不敏感）：</p><table tabindex="0"><thead><tr><th>策略码</th><th>判定</th></tr></thead><tbody><tr><td>空</td><td>允许（未配置 ABAC 策略）</td></tr><tr><td><code>allow</code></td><td>无条件允许</td></tr><tr><td><code>same_tenant</code> / <code>tenant_match</code></td><td>主体 <code>tenant_id</code> 与资源的 <code>tenant_id</code> / <code>route.tenant_id</code> / <code>query.tenant_id</code> 任一相等</td></tr><tr><td><code>self_only</code> / <code>owner_match</code></td><td>主体 <code>user_id</code> 与资源的 <code>user_id</code> / <code>owner_user_id</code> / <code>route.user_id</code> / <code>query.user_id</code> 任一相等</td></tr><tr><td>比较表达式</td><td>形如 <code>subject.x == resource.y</code>、<code>environment.utc_hour != &quot;0&quot;</code>；操作符 <code>==</code> / <code>!=</code> / <code>=</code>，操作数支持 <code>subject.</code> / <code>resource.</code> / <code>environment.</code> 前缀取值或字面量（字符串/布尔/数字）</td></tr><tr><td>其它</td><td>拒绝（不支持的策略）</td></tr></tbody></table><p><code>DefaultAbacAttributeCollector</code> 收集的属性包括：主体 <code>user_id</code>/<code>tenant_id</code>/<code>roles</code>/<code>is_authenticated</code> 及所有 <code>claim.*</code>；资源 <code>resource_type</code> 与反射得到的公共属性，若资源能解析出 <code>HttpContext</code> 还含 <code>route.*</code>/<code>query.*</code>/<code>http.method</code>/<code>http.path</code>/<code>http.client_ip</code>；环境 <code>permission_code</code>/<code>policy_code</code>/<code>utc_now</code>/<code>utc_hour</code>/<code>day_of_week</code>（有 <code>HttpContext</code> 时另含 <code>client_ip</code>/<code>request_path</code>/<code>request_method</code>/<code>user_agent</code>）。</p><h2 id="使用示例" tabindex="-1">使用示例 <a class="header-anchor" href="#使用示例" aria-label="Permalink to &quot;使用示例&quot;">​</a></h2><p>在服务里检查权限：</p><div class="language-csharp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">csharp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DocumentService</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">IPermissionChecker</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> permissions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Task</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DeleteAsync</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">long</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> userId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">long</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> docId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> permissions.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">IsGrantedAsync</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(userId.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ToString</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(), </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;document:delete&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">            throw</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> UnauthorizedAccessException</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>用特性声明式保护 Controller/Action（无需预注册命名策略）：</p><div class="language-csharp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">csharp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 仅权限码</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">PermissionAuthorize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;document:delete&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)]</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> IActionResult</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Delete</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">long</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ..</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 权限码 + ABAC：既要有权限，又要同租户</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">PermissionAuthorize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;document:read&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;same_tenant&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)]</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> IActionResult</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">long</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ..</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 仅 ABAC：只能操作本人资源</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">AbacAuthorize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;self_only&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)]</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> IActionResult</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> UpdateProfile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">long</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> userId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ..</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.;</span></span></code></pre></div><p>用策略门面判定（可带资源对象供 ABAC/Policy 使用）：</p><div class="language-csharp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">csharp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ReportService</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">IAuthorizationService</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> authz</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Task</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">bool</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">CanExportAsync</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">string</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> userId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ReportEntity</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> report</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        var</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> r</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> authz.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">AuthorizePolicyAsync</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(userId, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;report:export-policy&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, report);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> r.Succeeded;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><blockquote><p><code>XiHan.BasicApp</code> 在此基础上落地了完整的权限码 <code>resource:action:scope</code>、数据范围与字段级脱敏（FLS），见 <a href="https://basicapp.docs.xihanfun.com/backend/permission" target="_blank" rel="noreferrer">BasicApp 权限模型</a>。</p></blockquote><h2 id="扩展点-自定义" tabindex="-1">扩展点 / 自定义 <a class="header-anchor" href="#扩展点-自定义" aria-label="Permalink to &quot;扩展点 / 自定义&quot;">​</a></h2><ul><li><strong>权限 / 角色 / 策略存储</strong>：默认实现（<code>DefaultPermissionStore</code> / <code>DefaultRoleStore</code> / <code>DefaultPolicyStore</code>）基于 <code>ConcurrentDictionary</code> 的线程安全内存实现，可正常增删查（含各自的 <code>ClearAsync</code> 及 <code>AddRolesAsync</code> / <code>AddPermissionsAsync</code> / <code>AddPoliciesAsync</code> 等批量播种方法，便于开发/测试播种数据），但不持久化、不预置数据；生产必须实现 <code>IPermissionStore</code> / <code>IRoleStore</code> / <code>IPolicyStore</code> 读真实库，DI 覆盖（扩展方法用 <code>TryAddScoped</code>，先注册即生效）。</li><li><strong>超管通配 <code>*</code></strong>：由你实现的 <code>IPermissionChecker</code> / <code>IPermissionStore</code> 快照负责（如超管权限集含 <code>*</code>），本包不硬编码超管身份。</li><li><strong>自定义 ABAC 逻辑</strong>：实现 <code>IAbacEvaluator</code> 覆盖 <code>DefaultAbacEvaluator</code>，接你自己的策略引擎（如 OPA/规则 DSL）；或实现 <code>IAbacAttributeCollector</code> 补充领域特有的资源属性。</li><li><strong>自定义 Policy 要求</strong>：实现 <code>IAuthorizationRequirement</code>（Policies 版）放进 <code>PolicyDefinition.CustomRequirements</code>。</li></ul><h2 id="注意事项与最佳实践" tabindex="-1">注意事项与最佳实践 <a class="header-anchor" href="#注意事项与最佳实践" aria-label="Permalink to &quot;注意事项与最佳实践&quot;">​</a></h2><ul><li><strong>实时校验优先，不信任冻结声明</strong>：<code>HybridPermissionAuthorizationHandler</code> 一律以 <code>IPermissionChecker</code> 为准，故授权/撤销、禁用、注销即时生效——不要退回到只读 token 里的权限声明。</li><li><strong>策略名有 URL 编码</strong>：权限码/ABAC 码里含 <code>;</code>、<code>=</code>、空格等会被 <code>Uri.EscapeDataString</code> 编码进策略名；直接手写策略名时须遵循 <code>xihan.hybrid:p=...;a=...</code> 格式（一般用特性即可，无需手写）。</li><li><strong>ABAC 需要资源对象</strong>：<code>same_tenant</code>/<code>self_only</code>/比较表达式依赖资源属性；Controller 上要让 ABAC 拿到 <code>HttpContext</code>（<code>route.*</code>/<code>query.*</code>）或显式把资源对象作为授权 <code>resource</code> 传入。</li><li><strong>两个同名 <code>IAuthorizationRequirement</code></strong>：Policy 子系统的自定义要求 vs ASP.NET Core 的标记接口，命名空间不同、用途不同，勿混用。</li><li><strong>默认存储不可用于生产</strong>：默认内存存储不持久化、不预置数据；若不接真实 <code>IPermissionStore</code>/<code>IRoleStore</code> 且从未通过其 API 授予过数据，所有权限判定都会失败。</li></ul><h2 id="依赖模块" tabindex="-1">依赖模块 <a class="header-anchor" href="#依赖模块" aria-label="Permalink to &quot;依赖模块&quot;">​</a></h2><ul><li><a href="./core">XiHan.Framework.Core</a></li><li><a href="./authentication">XiHan.Framework.Authentication</a>（并传递依赖 <a href="./security">Security</a>）</li><li>框架引用：<strong>Microsoft.AspNetCore.App</strong>（ASP.NET Core 授权管道）</li></ul><h2 id="相关模块" tabindex="-1">相关模块 <a class="header-anchor" href="#相关模块" aria-label="Permalink to &quot;相关模块&quot;">​</a></h2><ul><li><a href="./authentication">XiHan.Framework.Authentication</a></li><li><a href="./security">XiHan.Framework.Security</a></li><li><a href="https://basicapp.docs.xihanfun.com/backend/permission" target="_blank" rel="noreferrer">BasicApp 权限模型</a>（权限码 / 数据范围 / 字段脱敏的应用层落地）</li></ul>`,55)])])}const u=i(a,[["render",d]]);export{k as __pageData,u as default};
