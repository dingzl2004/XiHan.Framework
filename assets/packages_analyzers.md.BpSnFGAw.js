import{_ as a,o as s,c as i,a3 as t}from"./chunks/framework.BLaSdaBb.js";const k=JSON.parse('{"title":"XiHan.Framework.Analyzers","description":"","frontmatter":{},"headers":[],"relativePath":"packages/analyzers.md","filePath":"packages/analyzers.md","lastUpdated":1786268168000}'),o={name:"packages/analyzers.md"};function n(d,e,l,r,c,h){return s(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="xihan-framework-analyzers" tabindex="-1">XiHan.Framework.Analyzers <a class="header-anchor" href="#xihan-framework-analyzers" aria-label="Permalink to &quot;XiHan.Framework.Analyzers&quot;">​</a></h1><blockquote><p>基于 Roslyn 的编译期规范分析器：检查 C# 源文件的曦寒标准版权文件头，并提供一键修复。</p></blockquote><ul><li><strong>NuGet</strong>：<code>XiHan.Framework.Analyzers</code></li><li><strong>模块类</strong>：—（作为 Analyzer 引用，编译期生效，无运行时模块类）</li><li><strong>所在层</strong>：工具 / 分析器</li><li><strong>关键依赖</strong>：Microsoft.CodeAnalysis.*（Roslyn，开发期依赖，<code>PrivateAssets=&quot;all&quot;</code>）</li></ul><h2 id="概述" tabindex="-1">概述 <a class="header-anchor" href="#概述" aria-label="Permalink to &quot;概述&quot;">​</a></h2><p>XiHan.Framework.Analyzers 是一套基于 Roslyn 的代码分析器与代码修复器，在<strong>编译期</strong>扫描 C# 源码是否符合框架规范，并在 IDE 中给出诊断提示和 Code Fix 一键修复。当前落地的规则是文件头版权声明检查（<code>XHFH001</code>）。它以 <code>netstandard2.0</code> 构建（Roslyn 分析器约定的目标框架），标记为开发期依赖（<code>DevelopmentDependency</code>），并按 analyzer 约定打包到 <code>analyzers/dotnet/cs</code>——因此只在开发/构建阶段生效，<strong>不进入运行时</strong>、不给消费方带来运行时依赖。</p><h2 id="何时使用" tabindex="-1">何时使用 <a class="header-anchor" href="#何时使用" aria-label="Permalink to &quot;何时使用&quot;">​</a></h2><ul><li>想在编码/编译阶段自动强制统一的文件头规范，把问题拦在提交与 CI 之前。</li><li>希望 IDE 直接给出诊断提示，并提供自动修复而不必手工补写文件头。</li><li>团队协作下保证所有 <code>.cs</code> 文件的版权/作者/GUID/创建时间声明一致。</li></ul><h2 id="安装与启用" tabindex="-1">安装与启用 <a class="header-anchor" href="#安装与启用" aria-label="Permalink to &quot;安装与启用&quot;">​</a></h2><p>作为分析器引用，引用后编译期<strong>自动生效</strong>，无需 <code>[DependsOn]</code> 或任何运行时接入。</p><p>发布后可通过 NuGet 包接入：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">dotnet</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> package</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> XiHan.Framework.Analyzers</span></span></code></pre></div><p>开发期也可通过项目引用以 analyzer 形式接入（推荐显式声明为 analyzer，避免把它当普通程序集引用）：</p><div class="language-xml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">xml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">ProjectReference</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Include</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;..\\XiHan.Framework.Analyzers\\XiHan.Framework.Analyzers.csproj&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">                  OutputItemType</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Analyzer&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">                  ReferenceOutputAssembly</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;false&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">                  PrivateAssets</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;all&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /&gt;</span></span></code></pre></div><p>启用后你得到：一个 <code>DiagnosticAnalyzer</code>（逐文件语法树扫描并产出诊断）与一个 <code>CodeFixProvider</code>（一键补写/替换文件头，支持 IDE 的 Fix All 批量修复）。</p><h2 id="工作原理" tabindex="-1">工作原理 <a class="header-anchor" href="#工作原理" aria-label="Permalink to &quot;工作原理&quot;">​</a></h2><ul><li>分析器注册 <code>SyntaxTree</code> 级动作，逐个 C# 源文件检查，并启用并发执行；不分析生成代码（<code>GeneratedCodeAnalysisFlags.None</code>）。</li><li><strong>跳过规则</strong>：非 <code>.cs</code> 文件、<code>bin</code>/<code>obj</code> 目录下的文件、<code>*.g.cs</code> / <code>*.generated.cs</code> / <code>*.designer.cs</code>、以及文件开头含 <code>&lt;auto-generated</code> 标记的文件都会被跳过。</li><li><strong>校验规则</strong>：文件必须以固定的两行版权与 MIT 授权声明开头；比对前会归一化行尾并剥离 BOM，再逐字符匹配。不满足即报 <code>XHFH001</code>。</li><li><strong>修复行为</strong>：Code Fix 会把文件顶部以 <code>// Copyright</code> 起始的连续注释行（含其后的一个空行）整体替换为标准两行文件头；若识别不到既有版权头，则直接在文件开头插入。</li></ul><h2 id="规则" tabindex="-1">规则 <a class="header-anchor" href="#规则" aria-label="Permalink to &quot;规则&quot;">​</a></h2><table tabindex="0"><thead><tr><th>规则 ID</th><th>标题</th><th>类别</th><th>默认级别</th><th>说明</th></tr></thead><tbody><tr><td><code>XHFH001</code></td><td>缺少曦寒标准版权文件头</td><td><code>XiHan.FileHeader</code></td><td><code>Warning</code>（默认启用）</td><td>C# 源文件必须以标准的两行版权与 MIT 授权声明开头；缺失或不合规即触发，消息为「文件 &#39;{0}&#39; 缺少或未正确声明曦寒标准版权文件头」。</td></tr></tbody></table><blockquote><p>目前源码中仅定义此一条规则（<code>XiHanFileHeaderRule.cs</code>）。</p></blockquote><h2 id="主要-api-类型" tabindex="-1">主要 API / 类型 <a class="header-anchor" href="#主要-api-类型" aria-label="Permalink to &quot;主要 API / 类型&quot;">​</a></h2><table tabindex="0"><thead><tr><th>类型</th><th>说明</th></tr></thead><tbody><tr><td><code>XiHanFileHeaderAnalyzer</code></td><td><code>public sealed</code>，<code>[DiagnosticAnalyzer(LanguageNames.CSharp)]</code>，注册语法树动作产出 <code>XHFH001</code> 诊断</td></tr><tr><td><code>XiHanFileHeaderCodeFixProvider</code></td><td><code>public sealed</code>，<code>[ExportCodeFixProvider]</code> + <code>[Shared]</code>，提供「添加曦寒标准版权文件头」修复，<code>GetFixAllProvider()</code> 返回 <code>BatchFixer</code> 支持批量修复</td></tr></tbody></table><blockquote><p><code>XiHanFileHeaderRule</code>（诊断描述符/ID/类别）与 <code>XiHanFileHeader</code>（文件头校验、解析、生成的内部工具）均为 <code>internal</code>，不属于对外 API。</p></blockquote><h2 id="配置" tabindex="-1">配置 <a class="header-anchor" href="#配置" aria-label="Permalink to &quot;配置&quot;">​</a></h2><p>分析器无 Options / 配置节。诊断级别通过 <code>.editorconfig</code> 的 <code>dotnet_diagnostic.&lt;ID&gt;.severity</code> 逐条调整，支持按目录/文件模式覆盖：</p><div class="language-ini vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ini</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 默认作为警告</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">[*.cs]</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">dotnet_diagnostic.XHFH001.severity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> = warning</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 需要在 CI 阻断时改为 error</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># dotnet_diagnostic.XHFH001.severity = error</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 对生成/迁移目录关闭</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">[**/Migrations/*.cs]</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">dotnet_diagnostic.XHFH001.severity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> = none</span></span></code></pre></div><h2 id="标准文件头格式" tabindex="-1">标准文件头格式 <a class="header-anchor" href="#标准文件头格式" aria-label="Permalink to &quot;标准文件头格式&quot;">​</a></h2><p><code>XHFH001</code> 校验并由 Code Fix 生成的文件头就是固定的两行，不含任何逐文件字段：</p><div class="language-csharp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">csharp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Copyright (c) 2021-Present XiHanFun and contributors.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Licensed under the MIT License. See LICENSE in the project root for license information.</span></span></code></pre></div><h2 id="扩展点-自定义" tabindex="-1">扩展点 / 自定义 <a class="header-anchor" href="#扩展点-自定义" aria-label="Permalink to &quot;扩展点 / 自定义&quot;">​</a></h2><ul><li>增加新规则：新增一个 <code>DiagnosticAnalyzer</code>（及可选 <code>CodeFixProvider</code>），沿用 <code>XiHanFileHeaderRule</code> 的模式定义唯一 <code>DiagnosticId</code> 与 <code>DiagnosticDescriptor</code>。因项目启用了 <code>EnforceExtendedAnalyzerRules</code>，需遵循分析器编写的扩展规则（如 <code>SupportedDiagnostics</code> 使用不可变数组、避免在分析器中做 I/O 等）。</li><li>消费方无需扩展即可用；调级别/排除目录一律通过 <code>.editorconfig</code>，不改分析器源码。</li></ul><h2 id="注意事项与最佳实践" tabindex="-1">注意事项与最佳实践 <a class="header-anchor" href="#注意事项与最佳实践" aria-label="Permalink to &quot;注意事项与最佳实践&quot;">​</a></h2><ul><li>只针对 C# 源文件；<code>bin</code>/<code>obj</code>、生成文件（<code>*.g.cs</code> / <code>*.generated.cs</code> / <code>*.designer.cs</code>、含 <code>&lt;auto-generated</code> 标记）会被自动跳过。</li><li>校验是<strong>严格匹配</strong>：两行内容完全固定，不含 <code>FileName</code> / <code>Guid</code> / <code>CreateTime</code> 等逐文件字段；比对前会归一化行尾并剥离 BOM，因此跨平台换行与 BOM 不影响判定。</li><li>项目以 <code>netstandard2.0</code> 构建、<code>IncludeBuildOutput=false</code> + <code>SuppressDependenciesWhenPacking=true</code> 打包为 analyzer，Roslyn 相关 <code>PackageReference</code> 全部 <code>PrivateAssets=&quot;all&quot;</code>，不会外泄给引用方，也不进入运行时。</li><li>引用它是开发期能力；要在 CI 阻断构建，请把 <code>XHFH001</code> 的严重级别调为 <code>error</code>。</li></ul><h2 id="依赖模块" tabindex="-1">依赖模块 <a class="header-anchor" href="#依赖模块" aria-label="Permalink to &quot;依赖模块&quot;">​</a></h2><p>无框架内部依赖。开发期依赖（均 <code>PrivateAssets=&quot;all&quot;</code>，不外泄给消费方）：</p><ul><li><code>Microsoft.CodeAnalysis.Analyzers</code></li><li><code>Microsoft.CodeAnalysis.CSharp.Workspaces</code></li><li><code>System.Composition.AttributedModel</code></li></ul><h2 id="相关模块" tabindex="-1">相关模块 <a class="header-anchor" href="#相关模块" aria-label="Permalink to &quot;相关模块&quot;">​</a></h2><ul><li><a href="./utils">XiHan.Framework.Utils</a> — 在编译期以 analyzer 形式引用本包做文件头规范检查。</li><li><a href="./core">XiHan.Framework.Core</a> — 框架核心库。</li></ul>`,37)])])}const g=a(o,[["render",n]]);export{k as __pageData,g as default};
