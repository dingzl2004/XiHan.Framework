import{_ as t,o as s,c as a,a3 as i}from"./chunks/framework.B5WAEDB7.js";const g=JSON.parse('{"title":"XiHan.Framework.Upgrade","description":"","frontmatter":{},"headers":[],"relativePath":"packages/upgrade.md","filePath":"packages/upgrade.md","lastUpdated":1786268168000}'),d={name:"packages/upgrade.md"};function o(n,e,r,c,l,p){return s(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="xihan-framework-upgrade" tabindex="-1">XiHan.Framework.Upgrade <a class="header-anchor" href="#xihan-framework-upgrade" aria-label="Permalink to &quot;XiHan.Framework.Upgrade&quot;">​</a></h1><blockquote><p>分布式安全升级引擎：检测版本 → 抢分布式锁 → 进维护模式 → 按序执行迁移脚本 → 回写版本 →（可选）替换文件 / 滚动重启，全流程编排 + 逐环节可插拔。</p></blockquote><ul><li><strong>NuGet</strong>：<code>XiHan.Framework.Upgrade</code></li><li><strong>模块类</strong>：<code>XiHanUpgradeModule</code></li><li><strong>所在层</strong>：基础设施层</li><li><strong>关键依赖</strong>：仅 .NET 原生 + 框架内部依赖（Core / MultiTenancy.Abstractions）。版本解析用自研 <code>SemanticVersion</code>。</li></ul><h2 id="概述" tabindex="-1">概述 <a class="header-anchor" href="#概述" aria-label="Permalink to &quot;概述&quot;">​</a></h2><p>XiHan.Framework.Upgrade 把分布式部署下的「安全升级」抽象成一套统一编排：应用启动或运维触发时，检测当前应用 / 数据库版本是否落后，落后则抢占分布式锁（保证集群内单节点执行），进入维护模式，按语义化版本顺序<strong>幂等</strong>执行 SQL 迁移脚本，回写版本记录，最后可选地替换程序文件、滚动重启。</p><p>它提供<strong>编排骨架 + 抽象 + 一批默认实现</strong>，但默认实现大多是 InMemory / Null 占位——<strong>版本存储、分布式锁、迁移执行三项生产环境必须替换为持久化 / 分布式实现</strong>（尤其迁移执行器默认直接抛异常，必须由应用层提供）。</p><h2 id="何时使用" tabindex="-1">何时使用 <a class="header-anchor" href="#何时使用" aria-label="Permalink to &quot;何时使用&quot;">​</a></h2><ul><li>应用启动或运维时，需判断当前应用 / 数据库版本是否落后并自动升级。</li><li>多节点部署下，需用分布式锁保证同一时刻只有一个节点执行升级。</li><li>按语义化版本发现、排序并<strong>幂等</strong>执行 SQL 迁移脚本，避免重复执行。</li><li>升级期间需要维护模式、程序文件替换、滚动重启等运维动作的统一编排。</li></ul><h2 id="安装与启用" tabindex="-1">安装与启用 <a class="header-anchor" href="#安装与启用" aria-label="Permalink to &quot;安装与启用&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">dotnet</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> package</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> XiHan.Framework.Upgrade</span></span></code></pre></div><div class="language-csharp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">csharp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">DependsOn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">typeof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">XiHanUpgradeModule</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))]</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> MyModule</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> : </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">XiHanModule</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { }</span></span></code></pre></div><p><code>XiHanUpgradeModule.ConfigureServices</code> 调用 <code>AddXiHanUpgrade(config)</code>，从配置节 <code>XiHan:Upgrade</code>（<code>XiHanUpgradeOptions.SectionName</code>）绑定选项，并为各扩展点注册默认实现（<code>TryAdd</code>，可被业务侧替换）：</p><table tabindex="0"><thead><tr><th>接口</th><th>默认实现</th><th>生命周期</th><th>生产可用性</th></tr></thead><tbody><tr><td><code>IUpgradeScriptProvider</code></td><td><code>FileSystemUpgradeScriptProvider</code></td><td>Singleton</td><td>可用（从文件系统扫描 SQL）</td></tr><tr><td><code>IUpgradeVersionStore</code></td><td><code>InMemoryUpgradeVersionStore</code></td><td>Scoped</td><td><strong>需替换为数据库实现</strong></td></tr><tr><td><code>IUpgradeLockProvider</code></td><td><code>InMemoryUpgradeLockProvider</code></td><td>Singleton</td><td><strong>需替换为分布式锁（进程内锁多节点无效）</strong></td></tr><tr><td><code>IUpgradeMigrationExecutor</code></td><td><code>DefaultUpgradeMigrationExecutor</code></td><td>Singleton</td><td><strong>必须替换</strong>（默认直接抛异常，见下）</td></tr><tr><td><code>IUpgradeTenantProvider</code></td><td><code>DefaultUpgradeTenantProvider</code></td><td>Scoped</td><td>视多租户需求替换（默认只返回当前 <code>ICurrentTenant</code>，不会遍历租户库）</td></tr><tr><td><code>IUpgradeStatusService</code></td><td><code>UpgradeStatusService</code></td><td>Scoped</td><td>可用</td></tr><tr><td><code>IUpgradeEngine</code></td><td><code>UpgradeEngine</code></td><td>Scoped</td><td>可用（编排核心）</td></tr><tr><td><code>IUpgradeCoordinator</code></td><td><code>UpgradeCoordinator</code></td><td>Singleton</td><td>可用</td></tr><tr><td><code>IUpgradeMaintenanceModeManager</code></td><td><code>DefaultUpgradeMaintenanceModeManager</code></td><td>Singleton</td><td>视需求替换</td></tr><tr><td><code>IUpgradeFileUpdater</code></td><td><code>NullUpgradeFileUpdater</code></td><td>Singleton</td><td>空实现（需要文件替换才换）</td></tr><tr><td><code>IRollingRestartCoordinator</code></td><td><code>NullRollingRestartCoordinator</code></td><td>Singleton</td><td>空实现（需要滚动重启才换）</td></tr></tbody></table><h3 id="启动自动检查" tabindex="-1">启动自动检查 <a class="header-anchor" href="#启动自动检查" aria-label="Permalink to &quot;启动自动检查&quot;">​</a></h3><p><code>XiHanUpgradeModule.OnPostApplicationInitializationAsync</code> 在应用初始化后运行，受 <code>XiHanUpgradeOptions.EnableAutoCheckOnStartup</code>（默认 <code>true</code>）门控：为 <code>true</code> 且已注册 <code>IUpgradeVersionStore</code> / <code>IUpgradeStatusService</code> 时，调用 <code>IUpgradeStatusService.EnsureInitializedAsync()</code> 完成初始化。<strong>注意：启动阶段只做「初始化」，真正执行升级由 <code>IUpgradeEngine.ExecuteAsync</code> / <code>IUpgradeCoordinator.StartAsync</code> 触发。</strong></p><h2 id="工作原理-upgradeengine-编排" tabindex="-1">工作原理（UpgradeEngine 编排） <a class="header-anchor" href="#工作原理-upgradeengine-编排" aria-label="Permalink to &quot;工作原理（UpgradeEngine 编排）&quot;">​</a></h2><p><code>UpgradeEngine.ExecuteAsync</code> 的核心流程（<code>ExecuteForTenantAsync</code>）：</p><ol><li><strong>主节点门控</strong>：若配置了 <code>PrimaryNodeName</code> 且当前节点非主节点，直接返回不升级。</li><li><strong>建表 + 取版本</strong>：<code>EnsureTablesAsync</code> → <code>GetOrCreateAsync</code> 取 / 建系统版本记录。</li><li><strong>收集脚本 + 比对版本</strong>：合并所有 <code>IUpgradeScriptProvider</code> 的脚本，取最新脚本版本；<code>SemanticVersion.Compare</code> 判定 <code>needDbUpgrade</code>（DbVersion &lt; 最新脚本版本）或 <code>needAppUpgrade</code>（记录 AppVersion &lt; 当前 App 版本）。都不需要则返回「无需升级」。</li><li><strong>抢锁</strong>：<code>TryAcquireLockAsync(resourceKey, expiry, nodeName)</code> 抢占带过期时间的资源锁；抢不到返回「升级锁已被占用」。多租户下锁键带 <code>:Tenant_{id}</code> 后缀。</li><li><strong>执行升级</strong>（锁作用域内，<code>IAsyncDisposable</code> 保证释放）：<code>SetUpgradingAsync</code> → 可选进维护模式 → <code>ExecuteMigrationsAsync</code> 迁移 → <code>SetUpgradeCompletedAsync</code> 回写版本 → 可选替换文件 → 退出维护模式 → 释放锁 → 可选滚动重启。</li><li><strong>迁移执行</strong>：脚本按版本分组、<code>SemanticVersion</code> 排序，同版本内按脚本名有序；逐条 <code>HasMigrationHistoryAsync</code> 去重（已执行则跳过，保证<strong>幂等</strong>），读文件内容交 <code>IUpgradeMigrationExecutor.ExecuteAsync(sql)</code> 执行，成功 / 失败都写 <code>UpgradeMigrationHistory</code>；任一脚本失败即抛出并 <code>SetUpgradeFailedAsync</code>。</li><li><strong>多租户隔离</strong>：<code>EnableMultiTenantIsolation=true</code> 时，遍历 <code>IUpgradeTenantProvider.GetTenants()</code>，逐租户 <code>ICurrentTenant.Change(...)</code> 后执行升级，任一租户失败即中止。<strong>注意</strong>：默认 <code>DefaultUpgradeTenantProvider</code> 只返回当前 <code>ICurrentTenant</code>（单个租户 / 宿主），并不会遍历租户库中的全部租户——要做「逐全体租户」批量升级需自行实现 <code>IUpgradeTenantProvider</code>，从租户仓储读取全部租户列表。</li></ol><p>版本解析：<code>AppVersion</code> 优先取选项，否则从入口程序集版本（<code>ReflectionHelper.GetEntryAssemblyVersion</code>）；<code>NodeName</code> 优先取选项，否则 <code>机器名-实例Id</code>。</p><h2 id="脚本组织约定-filesystemupgradescriptprovider" tabindex="-1">脚本组织约定（FileSystemUpgradeScriptProvider） <a class="header-anchor" href="#脚本组织约定-filesystemupgradescriptprovider" aria-label="Permalink to &quot;脚本组织约定（FileSystemUpgradeScriptProvider）&quot;">​</a></h2><p>默认脚本提供者从 <code>MigrationsRootPath</code>（默认 <code>migrations</code>，相对路径基于应用根目录 <code>AppContext.BaseDirectory</code>）扫描：</p><ul><li>根目录下每个<strong>子目录名即版本号</strong>（须能被 <code>SemanticVersion.TryParse</code> 解析，非法目录名跳过）。</li><li>每个版本目录内的 <code>*.sql</code> 文件为该版本的迁移脚本，按文件名有序执行。</li><li>整体先按版本语义排序、再按脚本名排序。</li></ul><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>migrations/</span></span>
<span class="line"><span>├── 1.0.0/</span></span>
<span class="line"><span>│   ├── 001_init.sql</span></span>
<span class="line"><span>│   └── 002_seed.sql</span></span>
<span class="line"><span>├── 1.1.0/</span></span>
<span class="line"><span>│   └── 001_add_column.sql</span></span>
<span class="line"><span>└── 2.0.0/</span></span>
<span class="line"><span>    └── 001_refactor.sql</span></span></code></pre></div><h2 id="核心能力" tabindex="-1">核心能力 <a class="header-anchor" href="#核心能力" aria-label="Permalink to &quot;核心能力&quot;">​</a></h2><ul><li><strong>版本存储与状态管理</strong>：<code>IUpgradeVersionStore</code> 建表、取 / 建版本记录、写升级中 / 完成 / 失败状态、追加并去重迁移历史。</li><li><strong>迁移执行（幂等）</strong>：按语义化版本分组排序脚本，逐条去重执行，历史留痕；执行经 <code>IUpgradeMigrationExecutor</code>（应保证事务）。</li><li><strong>分布式锁</strong>：<code>IUpgradeLockProvider</code> 抢占带过期的资源锁，<code>IUpgradeLockToken</code>（<code>IAsyncDisposable</code>）负责释放，确保集群内单节点升级。</li><li><strong>启动自动检查</strong>：受 <code>EnableAutoCheckOnStartup</code> 门控，在 <code>OnPostApplicationInitializationAsync</code> 初始化。</li><li><strong>语义化版本比较</strong>：<code>SemanticVersion</code> 提供解析与比较，用于强制升级判定与脚本排序。</li><li><strong>主节点 / 多租户编排</strong>：可配置仅主节点执行升级，支持逐租户隔离升级。</li><li><strong>运维扩展</strong>：维护模式、程序文件替换、滚动重启均为可插拔扩展点（默认 Null 空实现）。</li></ul><h2 id="主要-api-类型" tabindex="-1">主要 API / 类型 <a class="header-anchor" href="#主要-api-类型" aria-label="Permalink to &quot;主要 API / 类型&quot;">​</a></h2><table tabindex="0"><thead><tr><th>类型</th><th>关键成员 / 说明</th></tr></thead><tbody><tr><td><code>IUpgradeEngine</code> / <code>UpgradeEngine</code></td><td>升级引擎（编排核心）：<code>Task&lt;UpgradeStartResult&gt; ExecuteAsync(CancellationToken)</code></td></tr><tr><td><code>IUpgradeCoordinator</code> / <code>UpgradeCoordinator</code></td><td>升级协调器（后台启动）：<code>Task&lt;UpgradeStartResult&gt; StartAsync()</code>；内部用 <code>AsyncLock</code> 防重入，若上一次任务尚未完成，再次调用直接返回 <code>Started=false</code>、<code>Status=Upgrading</code> 而不会并发再起一个任务</td></tr><tr><td><code>IUpgradeStatusService</code> / <code>UpgradeStatusService</code></td><td>状态服务：<code>EnsureInitializedAsync()</code>、<code>GetVersionSnapshotAsync(clientVersion?, ...)</code>、<code>GetUpgradeStatusAsync(...)</code></td></tr><tr><td><code>IUpgradeVersionStore</code></td><td>版本 / 状态存储、迁移历史：<code>EnsureTablesAsync</code>、<code>GetOrCreateAsync</code>、<code>GetLatestHistoryAsync</code>、<code>SetUpgradingAsync</code>、<code>SetUpgradeCompletedAsync</code>、<code>SetUpgradeFailedAsync</code>、<code>UpdateDbVersionAsync</code>、<code>AddMigrationHistoryAsync</code>、<code>HasMigrationHistoryAsync</code></td></tr><tr><td><code>IUpgradeMigrationExecutor</code></td><td>迁移脚本执行器（内部保证事务）：<code>Task ExecuteAsync(string sql, CancellationToken)</code></td></tr><tr><td><code>IUpgradeScriptProvider</code></td><td>脚本发现来源：<code>Task&lt;IReadOnlyList&lt;UpgradeScript&gt;&gt; GetScriptsAsync(...)</code>（默认 <code>FileSystemUpgradeScriptProvider</code>）</td></tr><tr><td><code>IUpgradeLockProvider</code> / <code>IUpgradeLockToken</code></td><td>分布式锁：<code>TryAcquireLockAsync(resourceKey, expiry, nodeName, ...)</code>；令牌含 <code>ResourceKey</code>、<code>LockId</code>、<code>IsReleased</code>、<code>ReleaseAsync()</code>（<code>IAsyncDisposable</code>）</td></tr><tr><td><code>IUpgradeTenantProvider</code></td><td>多租户升级的租户来源：<code>GetTenants()</code></td></tr><tr><td><code>IUpgradeMaintenanceModeManager</code></td><td>维护模式：<code>EnterAsync(...)</code> / <code>ExitAsync(...)</code></td></tr><tr><td><code>IUpgradeFileUpdater</code> / <code>IRollingRestartCoordinator</code></td><td>程序文件替换 / 滚动重启（默认 Null 空实现）</td></tr><tr><td><code>SemanticVersion</code></td><td>语义化版本解析（<code>TryParse</code>）与比较（<code>Compare</code>）</td></tr><tr><td><code>UpgradeScript</code></td><td><code>record UpgradeScript(string Version, string ScriptName, string ScriptPath)</code></td></tr><tr><td><code>UpgradeStartResult</code></td><td><code>record</code>：<code>Started</code>、<code>Status</code>、<code>Message</code></td></tr><tr><td><code>UpgradeStatus</code></td><td>枚举：<code>Normal=0</code>、<code>Upgrading=1</code>、<code>Completed=2</code>、<code>Failed=3</code></td></tr><tr><td><code>UpgradeVersionState</code> / <code>UpgradeVersionSnapshot</code> / <code>UpgradeMigrationHistory</code></td><td>版本状态 / 快照 / 迁移历史模型</td></tr><tr><td><code>XiHanUpgradeOptions</code></td><td>升级选项（配置节 <code>XiHan:Upgrade</code>）</td></tr></tbody></table><h2 id="配置" tabindex="-1">配置 <a class="header-anchor" href="#配置" aria-label="Permalink to &quot;配置&quot;">​</a></h2><p>配置节：<code>XiHan:Upgrade</code>（<code>XiHanUpgradeOptions.SectionName</code>）。</p><table tabindex="0"><thead><tr><th>字段</th><th>类型</th><th>默认值</th><th>含义</th></tr></thead><tbody><tr><td><code>MinSupportVersion</code></td><td><code>string</code></td><td><code>&quot;0.0.0&quot;</code></td><td>最小支持版本（低于则强制升级）</td></tr><tr><td><code>AppVersion</code></td><td><code>string?</code></td><td><code>null</code></td><td>当前应用版本（为空时从入口程序集获取）</td></tr><tr><td><code>MigrationsRootPath</code></td><td><code>string</code></td><td><code>&quot;migrations&quot;</code></td><td>迁移脚本根目录（相对路径基于应用根目录）</td></tr><tr><td><code>LockResourceKey</code></td><td><code>string</code></td><td><code>&quot;SystemUpgrade&quot;</code></td><td>分布式锁资源键</td></tr><tr><td><code>LockExpirySeconds</code></td><td><code>int</code></td><td><code>600</code></td><td>分布式锁过期时间（秒）</td></tr><tr><td><code>EnableAutoCheckOnStartup</code></td><td><code>bool</code></td><td><code>true</code></td><td>启动时自动检查（初始化）</td></tr><tr><td><code>NodeName</code></td><td><code>string?</code></td><td><code>null</code></td><td>当前节点名（为空用「机器名-实例Id」）</td></tr><tr><td><code>PrimaryNodeName</code></td><td><code>string?</code></td><td><code>null</code></td><td>主节点名（配置后仅主节点可执行升级）</td></tr><tr><td><code>EnableMultiTenantIsolation</code></td><td><code>bool</code></td><td><code>false</code></td><td>是否逐租户隔离升级</td></tr><tr><td><code>ConnectionConfigId</code></td><td><code>string?</code></td><td><code>null</code></td><td>升级使用的数据库配置 Id（为空用默认连接）</td></tr><tr><td><code>EnableMaintenanceMode</code></td><td><code>bool</code></td><td><code>true</code></td><td>升级期间是否进入维护模式</td></tr><tr><td><code>EnableFileUpdate</code></td><td><code>bool</code></td><td><code>false</code></td><td>是否替换程序文件</td></tr><tr><td><code>EnableRollingRestart</code></td><td><code>bool</code></td><td><code>false</code></td><td>是否滚动重启</td></tr></tbody></table><p>示例 <code>appsettings.json</code>：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;XiHan&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;Upgrade&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;MinSupportVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1.0.0&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;MigrationsRootPath&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;migrations&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;LockResourceKey&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;SystemUpgrade&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;LockExpirySeconds&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">600</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;EnableAutoCheckOnStartup&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;PrimaryNodeName&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;node-01&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;EnableMaintenanceMode&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h2 id="使用示例" tabindex="-1">使用示例 <a class="header-anchor" href="#使用示例" aria-label="Permalink to &quot;使用示例&quot;">​</a></h2><h3 id="定义迁移步骤-sql-脚本-标准做法" tabindex="-1">定义迁移步骤（SQL 脚本，标准做法） <a class="header-anchor" href="#定义迁移步骤-sql-脚本-标准做法" aria-label="Permalink to &quot;定义迁移步骤（SQL 脚本，标准做法）&quot;">​</a></h3><p>框架的迁移「步骤」以文件系统 SQL 脚本承载——在 <code>migrations/&lt;版本号&gt;/&lt;序号_名称&gt;.sql</code> 放脚本即可（见上文脚本组织约定），引擎会自动发现、按序幂等执行。无需写代码类，脚本即步骤：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>migrations/1.2.0/001_add_index.sql</span></span>
<span class="line"><span>migrations/1.2.0/002_backfill.sql</span></span></code></pre></div><h3 id="应用层替换生产必需实现并手动触发升级" tabindex="-1">应用层替换生产必需实现并手动触发升级 <a class="header-anchor" href="#应用层替换生产必需实现并手动触发升级" aria-label="Permalink to &quot;应用层替换生产必需实现并手动触发升级&quot;">​</a></h3><div class="language-csharp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">csharp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> override</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ConfigureServices</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ServiceConfigurationContext</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> context</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    var</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> services</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> context.Services;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 三项生产必须替换：数据库版本存储 / 分布式锁 / 迁移执行器</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    services.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Replace</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ServiceDescriptor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Scoped</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">IUpgradeVersionStore</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">SqlSugarUpgradeVersionStore</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;());</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    services.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Replace</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ServiceDescriptor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Singleton</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">IUpgradeLockProvider</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">RedisUpgradeLockProvider</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;());</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    services.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Replace</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ServiceDescriptor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Singleton</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">IUpgradeMigrationExecutor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">SqlSugarMigrationExecutor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;());</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 运维触发一次升级（后台执行）</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Task</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">UpgradeStartResult</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TriggerUpgrade</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">IUpgradeCoordinator</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> coordinator</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    =&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> coordinator.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">StartAsync</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><h2 id="扩展点-自定义" tabindex="-1">扩展点 / 自定义 <a class="header-anchor" href="#扩展点-自定义" aria-label="Permalink to &quot;扩展点 / 自定义&quot;">​</a></h2><ul><li><strong>数据库版本存储</strong>：实现 <code>IUpgradeVersionStore</code>（建表 / 版本记录 / 状态 / 迁移历史），替换默认 <code>InMemoryUpgradeVersionStore</code>。</li><li><strong>分布式锁</strong>：实现 <code>IUpgradeLockProvider</code> + <code>IUpgradeLockToken</code>（如基于 Redis），替换默认进程内实现——<strong>多节点部署下这是必换项</strong>。</li><li><strong>迁移执行器</strong>：实现 <code>IUpgradeMigrationExecutor.ExecuteAsync(sql)</code> 并保证事务——<strong>必换</strong>（默认实现直接抛 <code>InvalidOperationException</code>）。</li><li><strong>脚本来源</strong>：多来源可用 <code>AddUpgradeScriptProvider&lt;T&gt;()</code> 追加（引擎会合并所有 provider 的脚本）；也可实现 <code>IUpgradeScriptProvider</code> 从数据库 / 嵌入资源读取。</li><li><strong>运维动作</strong>：需要维护模式 / 文件替换 / 滚动重启时，替换对应默认 / Null 实现（<code>IUpgradeMaintenanceModeManager</code> / <code>IUpgradeFileUpdater</code> / <code>IRollingRestartCoordinator</code>）。</li></ul><h2 id="注意事项与最佳实践" tabindex="-1">注意事项与最佳实践 <a class="header-anchor" href="#注意事项与最佳实践" aria-label="Permalink to &quot;注意事项与最佳实践&quot;">​</a></h2><ul><li><strong>默认 <code>IUpgradeMigrationExecutor</code> 会直接抛异常</strong>（&quot;未配置 IUpgradeMigrationExecutor 实现&quot;），未替换就启用数据库升级必失败——务必由应用层注册真实执行器。</li><li><strong>默认锁是进程内内存锁</strong>，多节点部署下形同虚设，无法保证「集群内单节点升级」——生产必须换成 Redis 等分布式锁。</li><li><strong>默认 <code>InMemoryUpgradeVersionStore</code> 虽注册为 Scoped，但内部用 <code>static</code> 字典（按租户键分区）保存版本状态与迁移历史</strong>，本质是进程级共享存储：同进程内跨请求 / 跨 Scope 均可见，但进程重启即丢失——这也是为什么它「生产环境必须换成数据库实现」。</li><li>迁移<strong>幂等</strong>依赖 <code>HasMigrationHistoryAsync</code> 去重，去重键是 <code>(version, scriptName)</code>——<strong>不要在已发布版本目录里改动已执行脚本的内容</strong>（内容变了但脚本名没变会被判为已执行而跳过）。</li><li>迁移脚本失败即中止并置失败状态，历史记录会留下 <code>ErrorMessage</code>；单版本内多脚本非全事务，注意脚本自身的可回滚 / 可重入设计。</li><li>单节点升级：不配 <code>PrimaryNodeName</code> 时每个节点都视为主节点，靠分布式锁串行化；配了则仅指定节点执行。</li><li>与本仓库既定策略一致：<strong>升级面向前向、部署重建数据库</strong>，不写向后兼容 / 迁移旧数据的兜底逻辑，遇异常态 fail-closed。</li></ul><h2 id="依赖模块" tabindex="-1">依赖模块 <a class="header-anchor" href="#依赖模块" aria-label="Permalink to &quot;依赖模块&quot;">​</a></h2><ul><li><a href="./core">XiHan.Framework.Core</a> — 模块化生命周期与依赖注入基础（<code>IApplicationInfoAccessor</code> 提供实例 Id）。</li><li><a href="./multitenancy-abstractions">XiHan.Framework.MultiTenancy.Abstractions</a> — 多租户抽象，支撑逐租户隔离升级（<code>ICurrentTenant.Change</code>）。</li></ul><h2 id="相关模块" tabindex="-1">相关模块 <a class="header-anchor" href="#相关模块" aria-label="Permalink to &quot;相关模块&quot;">​</a></h2><ul><li><a href="./data">XiHan.Framework.Data</a> — 数据访问层，业务侧通常在此实现版本存储与迁移执行器。</li><li><a href="./caching">XiHan.Framework.Caching</a> — 分布式锁底座，生产环境的升级锁常基于此实现。</li><li><a href="./timing">XiHan.Framework.Timing</a> — 时间处理，升级历史与状态记录常用。</li></ul>`,46)])])}const k=t(d,[["render",o]]);export{g as __pageData,k as default};
